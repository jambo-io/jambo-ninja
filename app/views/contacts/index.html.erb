<p id="notice"><%= notice %></p>


<div class="container mt-5">

  <strong>Ei!</strong> Quando tiver um tempinho, dê uma olhada nas <%= link_to "configurações" %> de envio. É fácil e rápido!

</div>

<div class="container container-main rounded-top mt-5">
  <div class="title-box">


    <div class="row">
      <div class="col-2" style="text-align: left;">
        <span id="title" >⚞ Contatos</span>
      </div>
      <div class="col-6" style="text-align: center;">

      </div>
      <div class="col-4" style="text-align: right;">
        <li class="list-inline-item"><%= link_to "Novo", new_contact_path, class: "btn btn-new float-right" %></li>
      </div>
    </div>

  </div>

  <table>
    <thead>
      <tr>
        <th>Nome</th>
        <th>Aniversário</th>
        <th>Email</th>
        <th>Celular</th>
        <th>Estado</th>
        <th>Endereço</th>
        <th>Número</th>
        <th>CEP</th>
        <th>Atividades</th>
        <th>Video</th>
        <th colspan="3"></th>
      </tr>
    </thead>

    <tbody>
      <% @contacts.each do |contact| %>
        <tr>
          <td><%= link_to "#{contact.name} #{contact.lastname}", contact_path(contact.id) %></td>
          <td><%= contact.birthday %></td>
          <td><%= contact.email %></td>
          <td><%= contact.phone %></td>
          <td><%= contact.state %></td>
          <td><%= contact.address %></td>
          <td><%= contact.number %></td>
          <td><%= contact.pc %></td>
          <td><%= contact.activities %></td>
          <td><%= contact.video %></td>
          <td><%= link_to 'Editar', edit_contact_path(contact) %></td>
          <td><%= link_to 'Destruir', contact, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        </tr>
      <% end %>
    </tbody>
  </table>

</div>
