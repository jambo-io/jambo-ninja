<%= form_for(administrative_region) do |f| %>
  <% if administrative_region.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(administrative_region.errors.count, "error") %> prohibited this administrative_region from being saved:</h2>

      <ul>
      <% administrative_region.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label "Nome da Região" %>
    <%= f.text_field :name, class:"form-control" %>
  </div>

  <div class="actions text-center">
    <%= f.submit "Salvar", class:'btn btn-success' %>
  </div>
<% end %>
