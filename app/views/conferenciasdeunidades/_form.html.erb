<%= form_for(conferenciasdeunidade) do |f| %>
  <% if conferenciasdeunidade.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(conferenciasdeunidade.errors.count, "error") %> prohibited this conferenciasdeunidade from being saved:</h2>

      <ul>
      <% conferenciasdeunidade.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :location %>
    <%= f.text_field :location %>
  </div>

  <div class="field">
    <%= f.label :start_at %>
    <%= f.date_select :start_at %>
  </div>

  <div class="field">
    <%= f.label :end_at %>
    <%= f.date_select :end_at %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_area :description %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
