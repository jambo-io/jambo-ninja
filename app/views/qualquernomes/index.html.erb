<p id="notice"><%= notice %></p>

<h1>Qualquernomes</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @qualquernomes.each do |qualquernome| %>
      <tr>
        <td><%= link_to 'Show', qualquernome %></td>
        <td><%= link_to 'Edit', edit_qualquernome_path(qualquernome) %></td>
        <td><%= link_to 'Destroy', qualquernome, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Qualquernome', new_qualquernome_path %>
