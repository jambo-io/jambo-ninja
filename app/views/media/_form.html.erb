<%= form_for(medium) do |f| %>
  <% if medium.errors.any? %>
    <div id="error_explanation">
      <h1><%= pluralize(medium.errors.count, "error") %> prohibited this medium from being saved:</h1>

      <ul>
      <% medium.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>

  <div class="field">
    <%= f.label :url %>
    <%= f.text_field :url %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
